# Golang Redis proxy server using gRPC

**Idea:**

A redis proxy server using gRPC in golang. You can check it by grpcui

**Usage:**
go-redis-grpc --redisaddr=[HOST:PORT] --listen=[PORT] --savelog=true|false